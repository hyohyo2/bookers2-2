<!--自分が送るメッセージ部分-->

<div class="right-container d-flex justify-content-end ">
  <p id="chat_<%= chat.id %>" style="background-color: lime; padding: 5px; border-radius: 10px; "><%= chat.message %></p>
  <%= button_to "削除", chat_path(chat), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-sm btn-danger ml-2",remote: true, id: "delete_chat_#{chat.id}" %>
</div>